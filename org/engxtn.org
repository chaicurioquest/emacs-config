#+TITLE: Engineering Extensions for Org Mode
#+STARTUP: indent
#+PROPERTY: header-args:emacs-lisp :tangle yes
#+DATE: 2025-10-20

This file contains engineering-specific Org extensions:
- TikZ / CircuitTikZ preview support
- Custom LaTeX preview preamble
- pdftoppm → PNG preview pipeline
- Mathpix OCR integration
- Export engine tuning

These should be loaded *after* general Org settings.

* Basic LaTeX Preview Settings
;; #+BEGIN_SRC emacs-lisp
;;(setq org-preview-latex-default-process 'pdftoppm) ;;; fallback if nothing better found
;; #+END_SRC


;; * Helper: Construct LaTeX Command
;; #+BEGIN_SRC emacs-lisp
;; (defun my/org-latex-make-latex-cmd (engine)
;;   "Return a LaTeX command for org-preview using ENGINE."
;;   (format "%s -interaction=nonstopmode -halt-on-error -output-directory=%%o %%f"
;;           (or engine "lualatex")))
;; #+END_SRC


* Ensure TeX Binaries Available
#+BEGIN_SRC emacs-lisp
(dolist (dir '("/usr/local/texlive/2025/bin/x86_64-linux"
               "/usr/bin"
               "/usr/local/bin"))
  (when (file-directory-p dir)
    (add-to-list 'exec-path dir)))

;; Only add the TeX Live path that is likely missing from the shell PATH
(let ((texlive-bin "/usr/local/texlive/2025/bin/x86_64-linux"))
  (when (and (file-directory-p texlive-bin)
             (not (string-match-p (regexp-quote texlive-bin)
                                  (or (getenv "PATH") ""))))
    (setenv "PATH" (concat texlive-bin ":" (getenv "PATH")))))
#+END_SRC


* LaTeX Preview Setup (Including TikZ + CircuitTikZ)
#+BEGIN_SRC emacs-lisp
    (setq org-format-latex-header
       "\\documentclass[varwidth=true,border=0pt]{standalone} % Fits image to content size
\\usepackage{fontspec}           % System font support (XeLaTeX/LuaLaTeX)
\\usepackage{amsmath,mathtools} % Standard math environments & extensions
\\DeclarePairedDelimiter{\\abs}{\\lvert}{\\rvert} % Smart |x|: use \\abs* for auto-scaling
\\usepackage{cancel}            % For striking through terms (e.g. \\cancel{L})
\\renewcommand{\\CancelColor}{\\color{red}} % Makes strike-through lines red
\\usepackage{xcolor}            % Basic color support
\\usepackage{siunitx}           % Professional unit/number formatting
\\usepackage{upgreek}           % Non-italicized Greek letters (\\upmu)
\\usepackage{tikz}              % Vector graphics and diagrams
\\usetikzlibrary{calc,arrows.meta,positioning} % Common TikZ utilities
\\usepackage[american]{circuitikz} % Circuit diagram support
\\usepackage[active,tightpage,displaymath,textmath]{preview} % Tight-crop fragment images

\\setlength\\PreviewBorder{0pt}  % No margin around preview images
\\PreviewEnvironment{math}       % Render $...$
\\PreviewEnvironment{displaymath} % Render \\[...\\]
\\PreviewEnvironment{equation}   % Render numbered equations
\\PreviewEnvironment{equation*}  % Render unnumbered equations
\\PreviewEnvironment{align}      % Render aligned equations
\\PreviewEnvironment{align*}     % Render unnumbered aligned equations
\\PreviewEnvironment{subequations} % Render grouped sub-equations
\\PreviewEnvironment{tikzpicture} % Render TikZ diagrams
\\PreviewEnvironment{circuitikz} % Render circuit diagrams

\\newcommand{\\pv}[1]{\\begin{preview}#1\\end{preview}} % Manual preview wrapper
\\mathsurround=0pt               % No extra space around math
\\setlength\\abovedisplayskip{0pt} % Tighten space above equations
\\setlength\\belowdisplayskip{0pt} % Tighten space below equations
\\setlength\\parindent{0pt}      % Disable paragraph indentation")
#+END_SRC


* Image Conversion Pipeline (pdftoppm → PNG)
#+BEGIN_SRC emacs-lisp
(let* ((engine   (or (bound-and-true-p my/org-latex-engine) "lualatex"))
       (pdftoppm (executable-find "pdftoppm"))
       (magick   (or (executable-find "magick") (executable-find "convert")))
       (gs       (executable-find "gs")))
  (cond
   ;; 1. Best: pdftoppm → super fast, crisp PNGs
   (pdftoppm
    (add-to-list 'org-preview-latex-process-alist
      '(pdftoppm-fast
       :programs ("lualatex" "pdftoppm")
        :description "Fast pdftoppm preview"
        :image-input-type "pdf"
        :image-output-type "png"
        :latex-compiler
          ("lualatex --shell-escape -interaction=nonstopmode -halt-on-error -output-directory %o %f")
        :image-converter
          ("pdftoppm -png -singlefile -r 150 %f %b" "mv %b-1.png %O")))
        (setq org-preview-latex-default-process 'pdftoppm-fast))

   ;; 2. Fallback: ImageMagick (convert/magick) + Ghostscript
   ((and magick gs)
    (add-to-list 'org-preview-latex-process-alist
                 '(imagemagick :programs ("lualatex" "convert")
                               :description "ImageMagick fallback"
                               :image-input-type "pdf"
                               :image-output-type "png"
                               :latex-compiler ("lualatex --shell-escape -interaction=nonstopmode -halt-on-error -output-directory %o %f")
                               :image-converter ("convert -density 300 %f -trim -flatten %O")))
    (setq org-preview-latex-default-process 'imagemagick))))
#+END_SRC

* Theme-Aware Preview Refresh
#+BEGIN_SRC emacs-lisp
(defun my/org-latex-preview-refresh ()
  "Clear preview cache and refresh when theme changes."
  (interactive)
  (shell-command "rm -rf /tmp/ltximg/")
  (when (derived-mode-p 'org-mode)
    (org-latex-preview '(64))))

(add-hook 'after-load-theme-hook #'my/org-latex-preview-refresh)
#+END_SRC


* Mathpix OCR Integration 
#+BEGIN_SRC emacs-lisp
;; Following is deferred for future enhancement since license & API is not availble for personal use.
;;(use-package mathpix
;;  :straight (:host github :repo "jethrokuan/mathpix.el")
;;  :custom
;;  (mathpix-app-id "YOUR_MATHPIX_APP_ID")
;;  (mathpix-app-key "YOUR_MATHPIX_APP_KEY")
;;  (mathpix-screenshot-method "scrot -s %s")
;;  :bind ("C-c x" . mathpix-screenshot)
;;  :config
;;  (add-hook 'mathpix-after-insert-hook #'org-latex-preview))
;;  (add-hook 'mathpix-after-insert-hook (lambda () (org-noter-sync-current-note)))  ;; Sync noter after OCR
#+END_SRC
